import React, { Component } from 'react';
import stowClient from './../Services/stowClient';

class RecordProcessing extends Component {
	componentDidMount = async () => {
		const { address, publicKey, data } = this.props;
		const stow = stowClient();

addRecordByProvider(
        bytes32 dataHash, address owner, string metadata, string dataUri)

	}
}